version: "3"
volumes:
    node_modules:
services:
  front:
    image: node:fermium
    volumes:
      - ./front/:/src
      - node_modules:/src/node_modules
    working_dir: /src
    command: tail -f /dev/null
    ports: 
      - 8080:8080